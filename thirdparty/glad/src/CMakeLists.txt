ADD_LIBRARY(glad STATIC glad.c)
TARGET_INCLUDE_DIRECTORIES(glad PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/../include)

IF("${CMAKE_CXX_COMPILER_ID}" STREQUAL "GNU")
    TARGET_LINK_LIBRARIES(glad PUBLIC -ldl)
ENDIF()
